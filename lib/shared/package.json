{
  "name": "@lib/shared",
  "version": "0.0.1",
  "license": "GPL-3.0",
  "author": "Elias Mawa <elias@emawa.io>",
  "private": true,
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/cjs/index.d.ts",
  "scripts": {
    "lint": "echo 'linting @lib/shared' && eslint . --ext .js,.ts",
    "prettier": "yarn prettier --write",
    "clean": "echo 'clean @lib/shared' && rm -rf ./dist** ||:",
    "rollup": "echo 'bundle @lib/shared' && rollup -c --bundleConfigAsCjs",
    "rollup:watch": "nodemon --watch 'src/' --delay 2000ms -e ts,tsx --exec yarn rollup",
    "watch": "echo 'watching @lib/shared' && nodemon --watch 'src/' -e ts,tsx --exec yarn build",
    "build": "NODE_ENV=production && yarn build:cjs",
    "build:cjs": "NODE_ENV=production && echo 'building @lib/shared:cjs' && tsc",
    "test": "jest"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "~15.0.1",
    "@rollup/plugin-typescript": "~10.0.1",
    "rollup": "~3.10.0",
    "rollup-plugin-dts": "~5.3.0",
    "rollup-plugin-terser": "~7.0.2",
    "ts-node": "~10.9.1",
    "typescript": "~4.9.4"
  }
}
